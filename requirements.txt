# Foundations Lab Dependencies
# Core algorithms and experiments are built strictly from first principles
# using NumPy and the Python standard library.
# Matplotlib is used solely for rendering experimental diagnostic plots into artifacts/.
# Pytest runs the automated verification suite.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pytest>=7.0
